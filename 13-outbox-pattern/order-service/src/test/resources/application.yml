server:
  port: 0

spring:
  application:
    name: order-service-test
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  kafka:
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        spring.json.type.mapping: orderCreated:com.playbook.orderservice.model.OrderCreatedEvent

management:
  endpoints:
    web:
      exposure:
        include: health
